# Must Package

Package must provides helpers that panic on error.

These functions simplify error handling in test code by reducing boilerplate
and eliminating manual error checking in test setup and assertions.

